# dépendances
node_modules/
.pnpm-store/

# build
.next/
out/
dist/
worker/dist/
*.tsbuildinfo

# secrets — invariant 6 de CLAUDE.md
.env
.env.*
!.env.example

# supabase local
supabase/.temp/
supabase/.branches/

# données de scan — des milliers d'images n'ont rien à faire dans git
inbox/
processed/
*.csv
tests/fixtures/golden/scans/

# divers
.DS_Store
coverage/
*.log

# fixtures de cartes: renders officiels sous copyright, cache local seulement
tests/fixtures/cards/

# données de langue tesseract, téléchargées au premier run (5 Mo)
*.traineddata

# généré par Next à chaque build
next-env.d.ts

# sauvegardes locales — jamais dans git
backups/

# cache du test de charge
.loadtest-cache/

# exports CSV générés
exports/

# vignettes générées à la volée
.thumb-cache/

# le golden set lui-même est commité (labels), seules les images ne le sont pas
!tests/fixtures/golden/labels.json
!tests/fixtures/golden/baseline.json
